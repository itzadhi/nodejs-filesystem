{
  "name": "nodejs-filesystem",
  "version": "1.0.0",
  "description": "To read all files in a folder and to create and write in a new file using current timestamp ",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "server": "nodemon index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "itzadhi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
